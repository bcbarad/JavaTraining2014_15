function showName (firstName, lastName){
var name = "Your name is ";
function makeFullName () {
return (name + firstName + " " + lastName);
	}
return makeFullName();
	}
function myFunction() {
    document.write(showName ("abcd", "efgh"));
}
